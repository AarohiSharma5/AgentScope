# Development & CI tooling for the backend and SDK.
# Install with:  pip install -r backend/requirements-dev.txt
-r requirements.txt

# Testing & coverage
pytest==8.2.2
pytest-cov==7.1.0

# Lint + formatting (ruff replaces flake8 + black + isort)
ruff==0.15.21

# Security scanning
bandit==1.9.4
pip-audit==2.7.3

# Python package build (for the SDK)
build==1.5.0
twine==6.2.0
