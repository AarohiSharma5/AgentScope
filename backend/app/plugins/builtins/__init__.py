"""Built-in example plugins shipped with AgentScope.

These demonstrate the plugin system across every capability type and are
discovered by importing this package (each module's plugin class self-registers
on import). They are intentionally dependency-free reference implementations;
real deployments can disable/uninstall them and drop in their own.
"""
